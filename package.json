{
  "name": "book-poc-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm run -r build",
    "test": "pnpm run -r test",
    "lint": "pnpm run -r lint",
    "gen": "pnpm --filter contracts gen"
  },
  "devDependencies": {
    "nx": "18.0.0"
  }
}
