{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter ./apps/web... exec next build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "rootDirectory": "apps/web"
}

