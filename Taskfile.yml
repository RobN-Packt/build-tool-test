version: "3"

vars:
  BACKEND_DIR: ./apps/api
  AWS_REGION: eu-north-1

includes:
  frontend: ./apps/web/Taskfile.yml
  backend: ./apps/api/Taskfile.yml
  lambda: ./apps/api/internal/lambda/Taskfile.yml

tasks:
  default:
    cmds: [task -l]
    silent: true

  ci:
    desc: Run tests + lint for all services
    cmds:
      - task frontend:ci
      - task backend:ci
      - task lambda:ci

  deploy:
    desc: Deploy web + api + lambda
    cmds:
      - task ci
      - task frontend:deploy
      - task backend:deploy
      - task lambda:deploy
